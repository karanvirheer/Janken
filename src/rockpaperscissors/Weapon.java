package rockpaperscissors;

/**
 * This is a superclass that compares each weapon and checks which is the
 * strongest. Implements the Comparable interface, which only has one method:
 * .compareTo(x)
 * 
 * @author Karanvir
 */
public abstract class Weapon implements Comparable<Weapon> {

}